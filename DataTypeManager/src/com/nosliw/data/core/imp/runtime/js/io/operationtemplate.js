var global;
function initForOperation(dataType, version){
	global = {
		dataType : {
			name : dataType,
			version : version,
		},
		operations: {}
	};
	return global.operations;
};

||operations||

global;
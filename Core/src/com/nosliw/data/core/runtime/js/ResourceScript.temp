nosliw.runtime.getResourceService().addResource(
function(){

	var resource = {
		type : '||type||',
		id : '||id||'		
	};

	resource.info = ||info||;
	resource.value = '||value||';

	return resource;
}());


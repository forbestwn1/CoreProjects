	var expressionService = nosliw.runtime.getExpressionService();
	expressionService.executeExecuteExpressionRequest(||expression||, ||variables||, {
		success : function(requestInfo, data){
			nosliw.getNodeData("||gatewayPath||").expressionExecuteResult("||taskId||", JSON.toString(data));
			nosliw.logging.info("Expression Result : ", data);
		}
	}, undefined);
			
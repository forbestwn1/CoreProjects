	var resourceIds = [||resourceIds||];
	
	var resourceService = nosliw.runtime.getResourceService();
	resourceService.executeGetResourcesRequest(resourceIds, {
		success : function(){
			aaaa.resourcesLoaded(||taskId||);
		}
	}, undefined);
